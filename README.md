# PCA

applied Principal Component Analysis (PCA) for dimensionality reduction and evaluated its impact using the K-Nearest Neighbors (KNN) algorithm.

Trained a KNN classifier on both original and reduced datasets
Compared model performance before and after PCA
